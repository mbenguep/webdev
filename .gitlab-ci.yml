image: docker:latest
services:
  - docker:dind

stages:
  - build
  
 before_script:
    - chmod +x mvnw

build:
  image: maven:3-jdk-8
  stage: build
  script:
    - ./mvnw package
  artifacts:
    paths:
      - build/libs/*.jar